{
  "name": "sentinel-agent",
  "version": "1.0.0",
  "description": "Sentinel privileged host agent — the only process with root/systemctl/Docker access. Exposes a fixed, schema-validated tool registry over a local Unix socket.",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "dockerode": "^4.0.2",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
