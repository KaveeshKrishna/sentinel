{
  "name": "sentinel-server",
  "version": "1.0.0",
  "description": "Sentinel unprivileged control plane — UI, API, WebSocket, auth, AI orchestration. Talks to sentinel-agent for all host access.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^9.4.3",
    "cookie-parser": "^1.4.6",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
